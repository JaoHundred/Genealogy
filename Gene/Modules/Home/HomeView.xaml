<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Gene.HomeView"
             xmlns:home="clr-namespace:Gene.Modules.Home"
             xmlns:loca="clr-namespace:Gene.Localization"
             x:DataType="home:HomeViewModel"
             Loaded="ContentPage_Loaded">

    <Grid>
        
        <CollectionView x:Name="colView">
            <!--emptyview is bugged-->
            
            <!--<CollectionView.EmptyView>
                <Label Text="{x:Static loca:Lang.EmptyList}" />
            </CollectionView.EmptyView>-->
        </CollectionView>
        <Label x:Name="workaroundEmptyView" Style="{StaticResource EmptyListStyle}"
               Text="{x:Static loca:Lang.EmptyList}" IsVisible="false"/>

        <Button Style="{StaticResource SoftSquareButton}" Text="+" Command="{Binding ButtonTestCommand}"/>
    </Grid>
</ContentPage>
